- main activity consists of the setup items (addr, port, version map)
    - service is created from main using a daw controller
- Create / bind to cixlistener service in the daw controller
    - Create a base class for all the other activities
        - connects to service through daw controller
- Daw controller
    - handles all communication with cixlistener
    - uses registered callbacks to update the ui
    - flags are more detailed the control surface
        - map each control in Ardour
        - use registered callbacks to modify strips and feedback in Ardour control surface command
            - only request feedback for registered controls
            - callbacks ignore what is not currently being displayed???
            - or each change in what's displayed initiates changes of callbacks???